# 旅游行业风控迁移说明

> 项目：AI_Risk 智能风控系统 —— **场景 A: 旅游风控** 迁移
> 依据：《AI_Risk_行业风控实战任务书》
> 迁移日期：2026-08-11
> 数据库：`ai_risk`（MySQL 8.0+，字符集 utf8mb4，root 密码 `123456`，应用账号 `app`/`app123`）

本文档详细说明将 AI_Risk 从电商（EC）风控迁移到旅游 OTA 风控的全部修改规则、
建表语句、规则配置与执行步骤。README.md 为项目整体使用指南，本文档专注于"行业迁移"本身。

---

## 一、迁移总览

| 项目 | 迁移前（电商） | 迁移后（旅游 OTA） |
|------|---------------|-------------------|
| 业务主体 | 电商平台买家 | OTA 平台旅行者/代订用户 |
| 业务单据 | 电商订单（实物商品） | 旅游预订订单（酒店/门票/跟团游/机票/火车票/租车） |
| 收货/履约 | 收货地址 + 物流发货 | 出行人/行程信息（receive_info 语义迁移） |
| 事件类型 | 下单 / 支付 / 售后申请 / 物流投诉 | **预订 / 支付 / 退改签 / 行程开始**（+规则面"通用"） |
| 规则分类 | 订单欺诈/支付风险/账户风险/售后风险/物流风险… | **预订欺诈/支付风险/账户风险/退改滥用/行程风险/票务风险** |
| 典型风险模式 | 黄牛抢购、恶意退货、盗刷 | **代订黑产、黄牛囤票、恶意退改、盗卡支付、行程投诉骗退、优惠滥用** |
| 特征体系 | 24 维（用户/订单/地址） | **25 维（用户 14 + 订单 8 + 行程 3）** |
| 风险规则 | 30 条电商规则 | **30 条旅游规则（R001-R030）** |

### 1.1 验收要求对照（任务书）

| 验收项 | 要求 | 完成情况 |
|--------|------|----------|
| 数据库表结构改造 | 业务表/风控表迁移为旅游语义 | ✅ 见第二、三章 |
| 行业模拟数据 ≥500 条 | 旅游场景模拟数据 | ✅ `gen_10w_data.py` 默认 1w 用户 × 1-8 单 ≈ 4.5w 订单 + 明细/退改/投诉；另有 6 演示用户、8 演示订单（`init_business_data.sql`）、30 风险用户（`gen_risky_users.py --count 30`） |
| 行业规则 ≥15 条 | 旅游风险规则 | ✅ **30 条**（R001-R030），远超要求 |
| 前端无电商术语 | 页面/下拉/标签全部旅游化 | ✅ 8 个模板 + static/app.js 已全部替换（见第六章） |
| Agent 理解旅游风控逻辑 | SYSTEM_PROMPT + 工具描述旅游化 | ✅ `app/agent/chat.py` / `app/agent/tools.py` |
| 端到端流程跑通 | 数据→规则→评分→案件→前端 | ✅ 见第八章执行步骤 |

---

## 二、术语映射表（全局替换规则）

以下为本次迁移采用的术语替换规则（代码、SQL、模板、脚本、文档统一执行）：

| 电商术语 | 旅游术语 | 影响范围 |
|----------|----------|----------|
| 下单 | 预订 | 事件类型、代码、模板、文档 |
| 售后申请 | 退改签 | 事件类型、代码、模板、文档 |
| 物流投诉 | 行程开始 | 事件类型、代码、模板、文档 |
| 订单欺诈 | 预订欺诈 | 规则分类 |
| 售后风险 | 退改滥用 | 规则分类 |
| 物流风险 | 行程风险 | 规则分类 |
| （新增） | 票务风险 | 规则分类 |
| 收货地址 | 出行人/行程信息（receive_info） | 业务表语义 |
| address_count（收货地址数） | trip_city_count（出行目的地城市数） | 用户画像字段 |
| addr_is_new（新地址） | trip_is_new_traveler（新出行人） | 特征 |
| 退货/换货/退货退款 | 退款/改期/退订 | postsale_type 枚举 |
| 配送/物流/自提 | 自由行/跟团游/定制游 | logistics_category 枚举 |
| 待发货/已发货/已签收 | 待支付/已支付/已确认/行程中/已完成/已取消/退改中 | order_status 枚举 |
| 电商风控系统 | 旅游风控系统 | 全局标题/文档 |
| 商品 | 旅游产品/SKU | 业务表注释 |

> 注：`receive_info`、`postsale`、`logistics` 等**表名保持不变**（沿用原系统表结构，
> 仅做语义迁移），避免 ORM/路由层大范围改名；其字段枚举与注释已全部旅游化。

---

## 三、数据库修改明细

### 3.1 风控表（risk_*，本项目自有表）

全部 9 张表重建/改造，建表语句见第四章：

| 表 | 修改内容 |
|----|----------|
| `risk_event` | `event_type` ENUM → `('预订','支付','退改签','行程开始')`；COMMENT 旅游化 |
| `risk_rule` | `rule_category` ENUM → 6 大类；`event_type` ENUM → 5 类（含"通用"）；`action` ENUM `('通过','标记','人工审核','拒绝')` |
| `risk_feature` | `entity_type` ENUM → `('用户','订单','行程','票券')` |
| `risk_assessment` | 结构不变 |
| `risk_case` | `event_type` ENUM → 4 类（含 source_id 业务追溯列） |
| `risk_blacklist` | 数据替换为旅游黑名单（代订黑产手机号/黄牛用户/盗卡用户） |
| `risk_user_profile` | 列 `address_count` → **`trip_city_count`**（出行目的地城市数） |
| `risk_action_log` | 结构不变（P4 审计表） |
| `risk_alert` | 结构不变（P4 告警表） |

### 3.2 业务表（本项目只读/演示用，共 17 张）

| 表 | 语义（旅游化后） | 修改内容 |
|----|-----------------|----------|
| `order_info` | 预订订单表 | **新增** `travel_date DATE`（出行日期）、`travel_persons INT`（出行人数）；`order_status` 字典值 → 旅游 7 态（待支付/已支付/已确认/行程中/已完成/已取消/退改中，`order_status` 字典表同步更新） |
| `postsale` | 退改签表 | `postsale_type` ENUM → `('退款','改期','退订')` |
| `logistics` | 行程履约表 | `logistics_category` ENUM → `('自由行','跟团游','定制游')` |
| `product_category` / `sku_info` | 旅游产品类别/SKU | 数据替换为 6 大旅游品类（酒店/门票/跟团游/机票/火车票/租车）与旅游 SKU |
| `region` | 区域表（目的地行政区划） | 数据替换为旅游目的地 |
| `receive_info` | 出行人/行程信息表 | 注释旅游化 |
| `user_info` / `order_detail` / `order_status` / `postsale_status` / `postsale_reason` / `logistics_company` / `order_logistics` / `logistics_complaint` / `logistics_complaints_record` / `postsale_logistics` | 其余表 | 结构不变，注释与演示数据旅游化 |

> 说明：业务表**表名全部保持不变**（如 `receive_info`=出行人/行程、`postsale`=退改签、
> `logistics`=行程履约、`logistics_company`=旅游供应商），仅做语义迁移，
> 避免 ORM/路由层大范围改名风险。

### 3.3 老库升级脚本

`sql/migration_travel_industry.sql` —— **老电商库一键升级**脚本：

1. `TRUNCATE` 全部业务表与风控表（清空电商数据）；
2. 幂等结构改造：`information_schema` 检查后执行
   `ALTER TABLE ... CHANGE COLUMN address_count trip_city_count ...`、
   `ADD COLUMN travel_date / travel_persons`；
3. `MODIFY COLUMN` 改写全部 ENUM 与 COMMENT。

**全新环境**直接用 `scripts/init_db.py --reset`（按
`init_business_tables.sql → init_business_data.sql → init_risk_tables.sql → init_risk_data.sql`
顺序执行），无需跑 migration 脚本。

---

## 四、建表语句

> 以下 DDL 直接摘自项目 SQL 源文件，为迁移后的最终结构（已在 MySQL 8.0.26 实测建库通过）。
> 数据库名沿用项目 `.env` 配置（DB_NAME=ecs，连接密码 123456），库内结构已全部旅游化。

### 4.1 风控表（9 张，来源 `sql/init_risk_tables.sql`）

**risk_event** —— 风控事件审计表 —— 每次风控检查落一条事件记录

```sql
CREATE TABLE IF NOT EXISTS `risk_event` (
    `event_id` VARCHAR(50) NOT NULL COMMENT '事件ID',
    `event_type` ENUM('预订','支付','退改签','行程开始') NOT NULL COMMENT '事件类型',
    `event_source_id` VARCHAR(50) NOT NULL COMMENT '关联业务ID',
    `user_id` VARCHAR(50) NOT NULL COMMENT '用户ID',
    `event_data` JSON COMMENT '事件快照',
    `create_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`event_id`),
    INDEX `idx_risk_event_user_id` (`user_id`),
    INDEX `idx_risk_event_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控事件审计表';
```

**risk_rule** —— 风控规则配置表 —— 30 条旅游规则（条件 JSON + 分值 + 动作）

```sql
CREATE TABLE IF NOT EXISTS `risk_rule` (
    `rule_id` VARCHAR(50) NOT NULL COMMENT '规则ID',
    `rule_name` VARCHAR(100) NOT NULL COMMENT '规则名称',
    `rule_category` ENUM('预订欺诈','支付风险','账户风险','退改滥用','行程风险','票务风险') NOT NULL COMMENT '风险场景分类',
    `event_type` ENUM('预订','支付','退改签','行程开始','通用') NOT NULL DEFAULT '通用' COMMENT '适用事件类型',
    `rule_condition` JSON NOT NULL COMMENT '条件表达式',
    `risk_level` ENUM('低','中','高','极高') NOT NULL COMMENT '风险等级',
    `risk_score` INT NOT NULL COMMENT '命中分值(0-100)',
    `action` ENUM('通过','标记','人工审核','拒绝') NOT NULL COMMENT '触发动作',
    `is_enabled` TINYINT(1) DEFAULT 1 COMMENT '是否启用',
    `priority` INT DEFAULT 0 COMMENT '优先级(越高越先执行)',
    `description` TEXT COMMENT '规则描述',
    `create_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    -- 【P3-M9 2026-08-07】软删: NULL=未删, 有值=删除时间
    `deleted_at` DATETIME DEFAULT NULL COMMENT '软删除时间(NULL=未删)',
    PRIMARY KEY (`rule_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控规则配置表';
```

**risk_blacklist** —— 风控黑名单表 —— 用户/地址/手机号三类，软删除

```sql
CREATE TABLE IF NOT EXISTS `risk_blacklist` (
    `blacklist_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '黑名单ID',
    `blacklist_type` ENUM('用户','地址','手机号') NOT NULL COMMENT '黑名单类型',
    `blacklist_value` VARCHAR(200) NOT NULL COMMENT '黑名单值',
    `reason` TEXT COMMENT '加入原因',
    `expire_time` DATETIME DEFAULT NULL COMMENT '过期时间(NULL=永久)',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    -- 【P3-M9 2026-08-07】软删: NULL=未删, 有值=删除时间 (撞黑检查/列表查都加 WHERE deleted_at IS NULL)
    `deleted_at` DATETIME DEFAULT NULL COMMENT '软删除时间(NULL=未删)',
    PRIMARY KEY (`blacklist_id`),
    UNIQUE INDEX `idx_blacklist_type_value` (`blacklist_type`, `blacklist_value`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控黑名单表';
```

**risk_user_profile** —— 用户风险画像表 —— 累计统计 + trip_city_count（出行目的地城市数）

```sql
CREATE TABLE IF NOT EXISTS `risk_user_profile` (
    `user_id` VARCHAR(50) NOT NULL COMMENT '用户ID',
    `risk_score` INT DEFAULT 0 COMMENT '综合风险评分(0-100)',
    `risk_level` ENUM('低','中','高','极高') DEFAULT '低' COMMENT '风险等级',
    `total_orders` INT DEFAULT 0 COMMENT '总预订订单数',
    `total_refunds` INT DEFAULT 0 COMMENT '退款/退订次数',
    `refund_rate` DECIMAL(5,4) DEFAULT 0 COMMENT '退款率',
    `avg_order_amount` DECIMAL(10,2) DEFAULT 0 COMMENT '平均订单金额',
    `trip_city_count` INT DEFAULT 0 COMMENT '出行目的地城市数量',
    `complaint_count` INT DEFAULT 0 COMMENT '行程投诉次数',
    `assessment_count` INT DEFAULT 0 COMMENT '评估次数',
    `last_assessment_time` DATETIME DEFAULT NULL COMMENT '最近评估时间',
    `profile_data` JSON COMMENT '扩展画像数据',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='用户风险画像表';
```

**risk_feature** —— 风控特征快照表 —— 事件级特征明细（entity_type: 用户/订单/行程/票券）

```sql
CREATE TABLE IF NOT EXISTS `risk_feature` (
    `feature_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '特征ID',
    `event_id` VARCHAR(50) NOT NULL COMMENT '关联事件ID',
    `entity_type` ENUM('用户','订单','行程','票券') NOT NULL COMMENT '实体类型',
    `entity_id` VARCHAR(50) NOT NULL COMMENT '实体ID',
    `feature_name` VARCHAR(100) NOT NULL COMMENT '特征名称',
    `feature_value` DECIMAL(15,4) COMMENT '特征值',
    `compute_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '计算时间',
    PRIMARY KEY (`feature_id`),
    INDEX `idx_risk_feature_event_id` (`event_id`),
    INDEX `idx_risk_feature_entity` (`entity_type`, `entity_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控特征快照表';
```

**risk_assessment** —— 风控评估结果表 —— 规则分 + ML 分 + 最终决策

```sql
CREATE TABLE IF NOT EXISTS `risk_assessment` (
    `assessment_id` VARCHAR(50) NOT NULL COMMENT '评估ID',
    `event_id` VARCHAR(50) NOT NULL COMMENT '关联事件ID',
    `user_id` VARCHAR(50) NOT NULL COMMENT '用户ID',
    `rule_results` JSON COMMENT '规则结果',
    `rule_count` INT DEFAULT 0 COMMENT '命中规则数',
    `final_score` INT NOT NULL COMMENT '最终评分(0-100)',
    `risk_level` ENUM('低','中','高','极高') NOT NULL COMMENT '风险等级',
    `decision` ENUM('通过','标记','人工审核','拒绝') NOT NULL COMMENT '决策',
    -- 【V2 2026-08-07】XGBoost 双轨融合字段: ml_score ∈ [0,1] = P(拒绝), ml_decision = ML 单维度决策
    `ml_score` DECIMAL(5,4) DEFAULT NULL COMMENT 'XGBoost 拒绝概率 [0,1](NULL=未加载)',
    `ml_decision` VARCHAR(10) DEFAULT NULL COMMENT 'ML 维度决策(通过/标记/人工审核/拒绝)',
    `create_time` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (`assessment_id`),
    INDEX `idx_risk_assessment_user_id` (`user_id`),
    INDEX `idx_risk_assessment_decision` (`decision`),
    INDEX `idx_risk_assessment_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控评估结果表';
```

**risk_case** —— 风控案件表 —— 高风险事件转案件人工审核

```sql
CREATE TABLE IF NOT EXISTS `risk_case` (
    `case_id` VARCHAR(50) NOT NULL COMMENT '案件ID',
    `assessment_id` VARCHAR(50) NOT NULL COMMENT '关联评估ID',
    `user_id` VARCHAR(50) NOT NULL COMMENT '用户ID',
    `case_status` ENUM('待审核','审核中','已通过','已拒绝','已关闭') NOT NULL DEFAULT '待审核' COMMENT '案件状态',
    `case_category` VARCHAR(50) DEFAULT NULL COMMENT '案件分类',
    `risk_detail` JSON COMMENT '风险详情',
    -- 【2026-08-07 补】业务回溯字段: decision.py 写入, "重做检查" 按钮回查用
    -- 之前漏在 DDL 里, 导致 ORM 查 risk_case.source_id 时报 1054 (修复: 合并自原 migration_add_case_source_id.sql)
    `source_id` VARCHAR(50) DEFAULT NULL COMMENT '原始业务ID(预订订单/退改签/行程投诉ID), 重做检查时用',
    `event_type` ENUM('预订','支付','退改签','行程开始') DEFAULT NULL COMMENT '触发案件的事件类型',
    `reviewer` VARCHAR(50) DEFAULT NULL COMMENT '审核人',
    `review_comment` TEXT COMMENT '审核意见',
    `review_time` DATETIME DEFAULT NULL COMMENT '审核时间',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (`case_id`),
    INDEX `idx_risk_case_status` (`case_status`),
    INDEX `idx_risk_case_user_id` (`user_id`),
    INDEX `idx_risk_case_source_id` (`source_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控案件表';
```

**risk_action_log** —— 风控操作审计日志表 —— 规则/案件/黑名单操作留痕（P4 系统表）

```sql
CREATE TABLE IF NOT EXISTS `risk_action_log` (
    `log_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '日志ID',
    `operator` VARCHAR(50) NOT NULL COMMENT '操作人(admin/system/ai_agent)',
    `action_type` ENUM('CREATE_RULE','UPDATE_RULE','TOGGLE_RULE','DELETE_RULE','REVIEW_CASE','AUTO_REJECT_CASE','AUTO_CLOSE_CASE','ADD_BLACKLIST','REMOVE_BLACKLIST') NOT NULL COMMENT '操作类型',
    `target_type` ENUM('rule','case','blacklist') NOT NULL COMMENT '对象类型',
    `target_id` VARCHAR(50) NOT NULL COMMENT '对象ID',
    `before_value` JSON COMMENT '变更前 (NULL=新增)',
    `after_value` JSON COMMENT '变更后 (NULL=删除)',
    `ip` VARCHAR(50) COMMENT '操作IP',
    `remark` VARCHAR(500) COMMENT '备注',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
    PRIMARY KEY (`log_id`),
    INDEX `idx_action_log_operator` (`operator`),
    INDEX `idx_action_log_target` (`target_type`, `target_id`),
    INDEX `idx_action_log_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控操作审计日志表';
```

**risk_alert** —— 风控告警记录表 —— 业务/模型/系统/安全四类告警（P4 系统表）

```sql
CREATE TABLE IF NOT EXISTS `risk_alert` (
    `alert_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '告警ID',
    `alert_type` ENUM('BUSINESS','MODEL','SYSTEM','SECURITY') NOT NULL COMMENT '告警分类',
    `alert_level` ENUM('P0','P1','P2','P3') NOT NULL COMMENT '告警等级 (P0=致命, P3=提示)',
    `alert_title` VARCHAR(200) NOT NULL COMMENT '告警标题',
    `alert_content` TEXT COMMENT '告警详情',
    `metric_name` VARCHAR(100) COMMENT '指标名(规则命中率/案件积压数等)',
    `metric_value` DECIMAL(20,6) COMMENT '触发值',
    `threshold` DECIMAL(20,6) COMMENT '阈值',
    `status` ENUM('PENDING','HANDLING','RESOLVED','IGNORED') DEFAULT 'PENDING' COMMENT '处理状态',
    `handler` VARCHAR(50) COMMENT '处理人',
    `resolve_time` DATETIME DEFAULT NULL COMMENT '解决时间',
    `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '告警时间',
    PRIMARY KEY (`alert_id`),
    INDEX `idx_alert_status` (`status`),
    INDEX `idx_alert_level` (`alert_level`),
    INDEX `idx_alert_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='风控告警记录表';
```

### 4.2 业务表（17 张，来源 `sql/init_business_tables.sql`）

**user_info** —— 用户信息表

```sql
CREATE TABLE IF NOT EXISTS `user_info` (
  `user_id` varchar(50) NOT NULL COMMENT '用户ID',
  PRIMARY KEY (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='用户信息表';
```

**region** —— 区域表（旅游目的地行政区划，省/市/区）

```sql
CREATE TABLE IF NOT EXISTS `region` (
  `province` varchar(20) NOT NULL COMMENT '省',
  `city` varchar(20) NOT NULL COMMENT '市',
  `district` varchar(20) NOT NULL COMMENT '区',
  PRIMARY KEY (`province`,`city`,`district`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='区域表';
```

**product_category** —— 旅游产品类别表（6 类）

```sql
CREATE TABLE IF NOT EXISTS `product_category` (
  `product_category` varchar(20) NOT NULL COMMENT '旅游产品类别',
  PRIMARY KEY (`product_category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='旅游产品类别表';
```

**order_status** —— 订单状态字典表（7 态）

```sql
CREATE TABLE IF NOT EXISTS `order_status` (
  `order_status` varchar(20) NOT NULL COMMENT '订单状态',
  `status_code` int DEFAULT NULL COMMENT '状态码',
  PRIMARY KEY (`order_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='订单状态表';
```

**logistics_company** —— 旅游供应商表（航司/酒店集团/票务平台/租车公司）

```sql
CREATE TABLE IF NOT EXISTS `logistics_company` (
  `company_name` varchar(20) NOT NULL COMMENT '供应商名称',
  PRIMARY KEY (`company_name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='旅游供应商表';
```

**postsale_status** —— 退改签状态字典表（9 态 + 是否退款/改期/退订标志）

```sql
CREATE TABLE IF NOT EXISTS `postsale_status` (
  `postsale_status` varchar(20) NOT NULL COMMENT '退改签状态',
  `is_refund` tinyint(1) NOT NULL COMMENT '是否退款',
  `is_return` tinyint(1) NOT NULL COMMENT '是否改期',
  `is_exchange` tinyint(1) NOT NULL COMMENT '是否退订',
  `status_code` int DEFAULT NULL COMMENT '状态码',
  PRIMARY KEY (`postsale_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='退改签状态表';
```

**receive_info** —— 出行人/行程信息表（原收货地址表，语义迁移）

```sql
CREATE TABLE IF NOT EXISTS `receive_info` (
  `receive_id` varchar(50) NOT NULL COMMENT '行程信息ID',
  `user_id` varchar(50) NOT NULL COMMENT '用户ID',
  `receiver_name` varchar(50) NOT NULL COMMENT '出行人姓名',
  `receiver_phone` varchar(50) NOT NULL COMMENT '出行人电话',
  `receive_province` varchar(50) NOT NULL COMMENT '目的地省',
  `receive_city` varchar(50) NOT NULL COMMENT '目的地市',
  `receive_district` varchar(50) NOT NULL COMMENT '目的地区',
  `receive_street_address` varchar(50) NOT NULL COMMENT '行程详细地址(酒店/景点)',
  PRIMARY KEY (`receive_id`),
  UNIQUE KEY `user_id` (`user_id`,`receiver_name`,`receiver_phone`,`receive_province`,`receive_city`,`receive_district`,`receive_street_address`),
  KEY `receive_province` (`receive_province`,`receive_city`,`receive_district`),
  CONSTRAINT `receive_info_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `user_info` (`user_id`),
  CONSTRAINT `receive_info_ibfk_2` FOREIGN KEY (`receive_province`, `receive_city`, `receive_district`) REFERENCES `region` (`province`, `city`, `district`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='出行人/行程信息表';
```

**sku_info** —— 旅游产品信息表（SKU：酒店/门票/跟团游/机票/火车票/租车）

```sql
CREATE TABLE IF NOT EXISTS `sku_info` (
  `sku_id` varchar(50) NOT NULL COMMENT '产品ID',
  `sku_name` varchar(100) NOT NULL COMMENT '产品名称',
  `sku_price` decimal(10,2) NOT NULL COMMENT '产品单价',
  `sku_category` varchar(20) NOT NULL COMMENT '产品类别',
  `sku_count` int NOT NULL COMMENT '产品库存(余票/余房)',
  PRIMARY KEY (`sku_id`),
  KEY `sku_category` (`sku_category`),
  CONSTRAINT `sku_info_ibfk_1` FOREIGN KEY (`sku_category`) REFERENCES `product_category` (`product_category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='旅游产品信息表';
```

**postsale_reason** —— 退改签原因字典表

```sql
CREATE TABLE IF NOT EXISTS `postsale_reason` (
  `postsale_reason` varchar(100) NOT NULL COMMENT '退改签原因',
  `product_category` varchar(20) DEFAULT NULL COMMENT '产品类别',
  PRIMARY KEY (`postsale_reason`),
  KEY `product_category` (`product_category`),
  CONSTRAINT `postsale_reason_ibfk_1` FOREIGN KEY (`product_category`) REFERENCES `product_category` (`product_category`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='退改签原因表';
```

**order_info** —— 预订订单表（新增 travel_date / travel_persons）

```sql
CREATE TABLE IF NOT EXISTS `order_info` (
  `order_id` varchar(50) NOT NULL COMMENT '订单ID',
  `create_time` timestamp NOT NULL COMMENT '创建时间(预订时间)',
  `payment_time` timestamp NULL DEFAULT NULL COMMENT '支付时间',
  `delivered_time` timestamp NULL DEFAULT NULL COMMENT '出行确认时间',
  `complete_time` timestamp NULL DEFAULT NULL COMMENT '完成时间',
  `user_id` varchar(50) NOT NULL COMMENT '用户ID',
  `receive_id` varchar(50) NOT NULL COMMENT '出行人/行程信息ID',
  `order_status` varchar(20) NOT NULL COMMENT '订单状态',
  `travel_date` date DEFAULT NULL COMMENT '出行日期',
  `travel_persons` int DEFAULT 1 COMMENT '出行人数',
  PRIMARY KEY (`order_id`),
  KEY `user_id` (`user_id`),
  KEY `receive_id` (`receive_id`),
  KEY `order_status` (`order_status`),
  CONSTRAINT `order_info_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `user_info` (`user_id`),
  CONSTRAINT `order_info_ibfk_2` FOREIGN KEY (`receive_id`) REFERENCES `receive_info` (`receive_id`),
  CONSTRAINT `order_info_ibfk_3` FOREIGN KEY (`order_status`) REFERENCES `order_status` (`order_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='预订订单表';
```

**logistics** —— 行程履约表（原物流表，语义迁移；category: 自由行/跟团游/定制游）

```sql
CREATE TABLE IF NOT EXISTS `logistics` (
  `logistics_id` varchar(50) NOT NULL COMMENT '行程ID',
  `create_time` timestamp NOT NULL COMMENT '创建时间',
  `delivered_time` timestamp NULL DEFAULT NULL COMMENT '行程完成时间',
  `logistics_tracking` varchar(500) DEFAULT NULL COMMENT '行程详细',
  `logistics_category` enum('自由行','跟团游','定制游') DEFAULT NULL COMMENT '出行方式',
  PRIMARY KEY (`logistics_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='行程履约表';
```

**order_detail** —— 订单明细表（产品数量单位：间夜/张/人次）

```sql
CREATE TABLE IF NOT EXISTS `order_detail` (
  `order_detail_id` varchar(50) NOT NULL COMMENT '订单明细ID',
  `order_id` varchar(50) NOT NULL COMMENT '订单ID',
  `sku_id` varchar(50) NOT NULL COMMENT '产品ID',
  `sku_name` varchar(100) NOT NULL COMMENT '产品名称',
  `sku_count` int NOT NULL COMMENT '产品数量(间夜/张/人次)',
  `total_amount` decimal(10,2) NOT NULL COMMENT '总计金额',
  `discount_amount` decimal(10,2) DEFAULT '0.00' COMMENT '优惠金额',
  `final_amount` decimal(10,2) NOT NULL COMMENT '实付金额',
  PRIMARY KEY (`order_detail_id`),
  KEY `sku_id` (`sku_id`),
  KEY `order_id` (`order_id`),
  CONSTRAINT `order_detail_ibfk_1` FOREIGN KEY (`sku_id`) REFERENCES `sku_info` (`sku_id`),
  CONSTRAINT `order_detail_ibfk_2` FOREIGN KEY (`order_id`) REFERENCES `order_info` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='订单明细表';
```

**order_logistics** —— 订单与行程关联表

```sql
CREATE TABLE IF NOT EXISTS `order_logistics` (
  `order_id` varchar(50) NOT NULL COMMENT '订单ID',
  `logistics_id` varchar(50) NOT NULL COMMENT '行程ID',
  PRIMARY KEY (`order_id`,`logistics_id`),
  KEY `logistics_id` (`logistics_id`),
  CONSTRAINT `order_logistics_ibfk_1` FOREIGN KEY (`order_id`) REFERENCES `order_info` (`order_id`),
  CONSTRAINT `order_logistics_ibfk_2` FOREIGN KEY (`logistics_id`) REFERENCES `logistics` (`logistics_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='订单与行程关联表';
```

**logistics_complaint** —— 行程投诉内容对照表

```sql
CREATE TABLE IF NOT EXISTS `logistics_complaint` (
  `logistics_status` varchar(20) NOT NULL COMMENT '行程状态',
  `logistics_complaint` varchar(100) NOT NULL COMMENT '行程投诉内容',
  PRIMARY KEY (`logistics_status`,`logistics_complaint`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='行程投诉内容对照表';
```

**logistics_complaints_record** —— 行程投诉记录表

```sql
CREATE TABLE IF NOT EXISTS `logistics_complaints_record` (
  `record_id` bigint NOT NULL AUTO_INCREMENT COMMENT '投诉记录ID',
  `logistics_id` varchar(50) NOT NULL COMMENT '行程ID',
  `logistics_complaint` varchar(500) NOT NULL COMMENT '行程投诉内容',
  `complaint_time` timestamp NOT NULL COMMENT '投诉时间',
  `user_id` varchar(50) NOT NULL COMMENT '用户ID',
  PRIMARY KEY (`record_id`),
  KEY `user_id` (`user_id`),
  KEY `logistics_id` (`logistics_id`),
  CONSTRAINT `logistics_complaints_record_ibfk_1` FOREIGN KEY (`user_id`) REFERENCES `user_info` (`user_id`),
  CONSTRAINT `logistics_complaints_record_ibfk_2` FOREIGN KEY (`logistics_id`) REFERENCES `logistics` (`logistics_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='行程投诉记录表';
```

**postsale** —— 退改签表（type: 退款/改期/退订）

```sql
CREATE TABLE IF NOT EXISTS `postsale` (
  `postsale_id` varchar(50) NOT NULL COMMENT '退改签ID',
  `create_time` timestamp NOT NULL COMMENT '创建时间',
  `complete_time` timestamp NULL DEFAULT NULL COMMENT '完成时间',
  `order_detail_id` varchar(50) NOT NULL COMMENT '订单明细ID',
  `refund_amount` decimal(10,2) DEFAULT '0.00' COMMENT '退款金额',
  `postsale_type` enum('退款','改期','退订') DEFAULT NULL COMMENT '退改签类型',
  `postsale_reason` varchar(500) NOT NULL COMMENT '退改签原因',
  `postsale_status` varchar(20) NOT NULL COMMENT '退改签状态',
  `receive_id` varchar(50) NOT NULL COMMENT '出行人/行程信息ID',
  PRIMARY KEY (`postsale_id`),
  KEY `receive_id` (`receive_id`),
  KEY `order_detail_id` (`order_detail_id`),
  KEY `postsale_status` (`postsale_status`),
  CONSTRAINT `postsale_ibfk_1` FOREIGN KEY (`receive_id`) REFERENCES `receive_info` (`receive_id`),
  CONSTRAINT `postsale_ibfk_2` FOREIGN KEY (`order_detail_id`) REFERENCES `order_detail` (`order_detail_id`),
  CONSTRAINT `postsale_ibfk_3` FOREIGN KEY (`postsale_status`) REFERENCES `postsale_status` (`postsale_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='退改签表';
```

**postsale_logistics** —— 退改签与行程关联表

```sql
CREATE TABLE IF NOT EXISTS `postsale_logistics` (
  `postsale_id` varchar(50) NOT NULL COMMENT '退改签ID',
  `logistics_id` varchar(50) NOT NULL COMMENT '行程ID',
  PRIMARY KEY (`postsale_id`,`logistics_id`),
  KEY `logistics_id` (`logistics_id`),
  CONSTRAINT `postsale_logistics_ibfk_1` FOREIGN KEY (`postsale_id`) REFERENCES `postsale` (`postsale_id`),
  CONSTRAINT `postsale_logistics_ibfk_2` FOREIGN KEY (`logistics_id`) REFERENCES `logistics` (`logistics_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='退改签与行程关联表';
```

---

## 五、风险规则清单（30 条，R001-R030）

规则数据位于 `sql/init_risk_data.sql`，按 6 大类组织。条件 JSON 支持操作符
`gt/ge/lt/le/eq/ne/between/in/and/or`；字段前缀 `order_*`（当单）/ `trip_*`（行程）/
`user_*`（用户画像聚合）/ `rule_*`（全局聚合）。

### 5.1 预订欺诈（7 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R001 | 大额预订 | `order_total_amount >= 8000` | +10 高 标记 |
| R002 | 超高额预订 | `order_total_amount >= 20000` | +35 极高 **拒绝** ⚡一票否决 |
| R003 | 高频预订 | `user_orders_7d >= 8` | +20 高 标记 |
| R004 | 极端高频预订 | `user_orders_7d >= 20` | +35 极高 **拒绝** ⚡一票否决 |
| R005 | 深夜预订 | `order_is_night == 1` | +10 中 标记 |
| R006 | 临期大额预订 | `order_total_amount >= 5000 AND order_lead_days <= 1` | +30 高 审核 |
| R007 | 多出行人大额 | `travel_persons >= 6 AND order_total_amount >= 10000` | +35 极高 **拒绝** ⚡一票否决 |

### 5.2 支付风险（3 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R008 | 极速支付 | `order_pay_interval_sec <= 3` | +15 中 标记 |
| R009 | 支付后频繁取消 | `user_cancel_count >= 3 AND user_orders_30d >= 5` | +20 中 标记 |
| R010 | 深夜高额支付 | `order_is_night == 1 AND order_total_amount >= 3000` | +20 高 审核 |

### 5.3 账户风险（6 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R011 | 高退改率 | `user_postsale_rate >= 0.3` | +20 高 标记 |
| R012 | 极高退改率 | `user_postsale_rate >= 0.8` | +35 极高 **拒绝** ⚡一票否决 |
| R013 | 高退款取消率 | `user_cancel_count >= 2 AND user_total_orders >= 10 AND user_postsale_count >= 3` | +15 高 标记 |
| R014 | 高频取消 | `user_cancel_count >= 5` | +20 高 审核 |
| R015 | 新账户大额 | `user_orders_30d <= 3 AND order_total_amount >= 10000` | +25 高 审核 |
| R016 | 多目的地行程 | `user_trip_city_count >= 3 AND user_orders_7d >= 5` | +20 高 标记 |

### 5.4 退改滥用（4 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R017 | 高退款金额 | `user_refund_amount >= 5000` | +25 高 审核 |
| R018 | 退改申请频繁 | `user_postsale_count >= 8` | +20 高 标记 |
| R019 | 累计高额退款 | `user_refund_count >= 5 AND user_refund_amount >= 10000` | +35 极高 **拒绝** ⚡一票否决 |
| R020 | 高频改期 | `user_postsale_count >= 4 AND user_refund_count >= 2 AND user_postsale_rate >= 0.5` | +20 高 标记 |

### 5.5 行程风险（3 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R021 | 投诉用户 | `user_complaint_count >= 2` | +10 中 标记 |
| R022 | 投诉过多 | `user_complaint_count >= 5` | +20 高 审核 |
| R023 | 投诉×高退改率 | `user_complaint_count >= 1 AND user_postsale_rate >= 0.5` | +25 高 审核 |

### 5.6 票务风险（7 条）

| 规则ID | 名称 | 条件 | 加分/等级/处置 |
|--------|------|------|----------------|
| R024 | 批量购票 | `order_item_count >= 10` | +25 高 标记 |
| R025 | 黄牛囤票 | `order_item_count >= 20 AND order_total_amount >= 10000` | +40 极高 **拒绝** ⚡一票否决 |
| R026 | 深折扣订单 | `order_discount_rate >= 0.5 AND order_discount_amount >= 1000` | +15 高 标记 |
| R027 | 折扣叠加高频 | `user_orders_7d >= 3 AND order_discount_rate >= 0.3` | +10 中 标记 |
| R028 | 多品类混合 | `order_sku_count >= 3 AND order_total_amount >= 5000` | +15 高 审核 |
| R029 | 新出行人大额 | `trip_is_new_traveler == 1 AND order_total_amount >= 8000` | +25 高 审核 |
| R030 | 高频大额代订 | `user_orders_7d >= 15 AND order_lead_days <= 2 AND user_avg_order_amount >= 5000` | +35 极高 **拒绝** ⚡一票否决 |

**一票否决规则（6 条）**：R002 / R004 / R012 / R019 / R025 / R030 —— 分数 ≥85 且 action=拒绝，
命中即最终拒绝（`decision.py: check_veto`）。

### 5.7 黑名单预置数据（5 条）

| 类型 | 值 | 原因 |
|------|-----|------|
| 手机号 | 13900009999 | 代订黑产团伙, 批量预订后加价转卖 |
| 手机号 | 13711112222 | 黄牛团伙, 多次囤积热门景区门票 |
| 手机号 | 13633334444 | 多次投诉后骗退全额, 已报警 |
| 用户 | U_BLACK_001 | 盗卡支付, 已封号 |
| 用户 | U_BLACK_002 | 恶意退改 47 次, 薅取优惠券 |

---

## 六、特征工程（25 维）

`app/engine/feature.py` + `app/engine/ml_model.py: FEATURE_COLUMNS` 严格对齐：

| 维度 | 特征名 | 含义 | 风控语义 |
|------|--------|------|----------|
| 用户1 | user_total_orders | 累计订单数 | 账户活跃度 |
| 用户2 | user_orders_7d | 近7天预订数 | 高频代订检测 |
| 用户3 | user_orders_30d | 近30天预订数 | 高频代订检测 |
| 用户4 | user_total_amount | 累计预订金额 | 账户价值 |
| 用户5 | user_avg_order_amount | 平均单笔金额 | 客单价异常 |
| 用户6 | user_max_order_amount | 最大单笔金额 | 大额预订历史 |
| 用户7 | user_refund_count | 退款次数 | 恶意退改检测 |
| 用户8 | user_postsale_count | 退改申请次数 | 恶意退改检测 |
| 用户9 | user_refund_rate | 退款率 | 恶意退改检测 |
| 用户10 | user_postsale_rate | 退改申请率 | 恶意退改检测 |
| 用户11 | user_refund_amount | 累计退款金额 | 恶意退改检测 |
| 用户12 | user_cancel_count | 订单取消次数 | 取消行为异常 |
| 用户13 | user_complaint_count | 行程投诉次数 | 投诉骗退检测 |
| 用户14 | user_trip_city_count | 出行目的地城市数 | 代订黑产检测（多城市） |
| 订单1 | order_total_amount | 订单总金额 | 当单金额 |
| 订单2 | order_item_count | 明细行数 | 囤票/批量检测 |
| 订单3 | order_sku_count | SKU去重数 | 囤票检测 |
| 订单4 | order_discount_amount | 优惠金额 | 优惠滥用检测 |
| 订单5 | order_discount_rate | 优惠比例 | 优惠滥用检测 |
| 订单6 | order_pay_interval_sec | 预订→支付间隔(秒) | 盗卡/脚本支付检测 |
| 订单7 | order_is_night | 深夜时段(0/1) | 深夜风险行为 |
| 订单8 | order_lead_days | 出行提前期(天) = travel_date − create_time | 临期代订/黄牛检测（旅游特有） |
| 行程1 | trip_traveler_count | 出行人数量 | 代订黑产检测（多出行人） |
| 行程2 | trip_city_count | 目的地城市数 | 代订黑产检测 |
| 行程3 | trip_is_new_traveler | 本单是否新出行人(0/1) | 新出行人大额=代订风险 |

---

## 七、事件类型与阈值

`app/config.py: RISK_EVENT_THRESHOLDS`（按事件类型动态调整决策阈值，每类 3 阈值
pass/mark/review；score ≥ review 直接"极高"）：

| 事件类型 | pass（通过线） | mark（标记线） | review（审核线） | 说明 |
|----------|:----:|:----:|:----:|------|
| 预订 | 30 | 60 | 80 | 标准 |
| 支付 | 25 | 55 | 75 | 支付更严（钱的事，阈值整体下调更敏感） |
| 退改签 | 40 | 70 | 85 | 退改更严（防薅羊毛，pass 线最高） |
| 行程开始 | 35 | 65 | 80 | 行程偏严 |
| 通用（默认） | 30 | 60 | 80 | = 全局默认（RISK_PASS/MARK/REVIEW_THRESHOLD） |

业务逻辑：退改签是"薅羊毛"高发环节，pass 阈值最高（39 分以下才算低风险）；
支付涉及资金安全，三档阈值整体下调，更早进入标记/审核。

风险等级划分（分数 → 等级）：低 0-29 / 中 30-59 / 高 60-84 / 极高 85-100。

---

## 八、代码修改清单

| 模块 | 文件 | 修改内容 |
|------|------|----------|
| ORM | `app/models_risk.py` | 4 处 ENUM 旅游化；`address_count` → `trip_city_count` |
| ORM | `app/models_business.py` | OrderInfo 新增 `travel_date`/`travel_persons`；postsale/logistics ENUM 旅游化 |
| 决策引擎 | `app/engine/decision.py` | 事件分支（预订/支付/退改签/行程开始）；画像字段；特征实体默认"行程" |
| 特征 | `app/engine/feature.py` | 25 维旅游特征（含 `order_lead_days`、`trip_*` 3 维） |
| 模型 | `app/engine/ml_model.py` | FEATURE_COLUMNS 对齐 25 维旅游特征 |
| 配置 | `app/config.py` | RISK_EVENT_THRESHOLDS 键改为旅游事件类型 |
| Schema | `app/schemas.py` | Literal 事件/分类枚举；UserProfile 字段改名 |
| 校验 | `app/service/validator.py` | 事件来源校验器键旅游化 |
| 补全 | `app/service/event.py` | 各事件分支字段补全（订单/退改/行程） |
| 案件 | `app/service/case.py` | `address_count` 引用改为 `trip_city_count` |
| Agent | `app/agent/chat.py` | SYSTEM_PROMPT 旅游 OTA 领域知识重写（代订黑产/黄牛囤票/恶意退改/盗卡支付/行程投诉骗退/优惠滥用） |
| Agent | `app/agent/tools.py` | 工具描述旅游化（事件类型/画像字段） |
| 前端 | `templates/*.html`（8 个） | 标题/下拉/标签全部旅游化 |
| 前端 | `static/app.js` | FEATURE_LABELS 25 维旅游特征；阈值键 |
| SQL | `sql/init_business_tables.sql` | 见第三、四章 |
| SQL | `sql/init_business_data.sql` | 旅游字典数据（品类/状态/目的地/SKU/演示订单） |
| SQL | `sql/init_risk_tables.sql` | 见第四章 |
| SQL | `sql/init_risk_data.sql` | 30 条规则 + 5 条黑名单 |
| SQL | `sql/migration_travel_industry.sql` | **新增**：老库一键升级脚本 |
| 脚本 | `scripts/gen_10w_data.py` | 旅游模拟数据生成（目的地与 region 表一致、含 travel_date/travel_persons/行程） |
| 脚本 | `scripts/gen_risky_users.py` | 5 类旅游风险模式（高退改率/高频预订/高退款金额/多目的地/有投诉）轮换生成 RISK001-RISK030 |
| 脚本 | `scripts/init_db.py` | 默认密码 123456；SQL 序列不变 |
| 测试 | `tests/test_gen_risky_users.py` | RISK_MODES 断言旅游化 |
| 测试 | `tests/test_rule_level_event_config.py` | 阈值键/特征前缀（trip_）/模板路径（改为项目相对路径） |
| 文档 | `README.md` | 标题/密码/脚本参数/规则表全面更新 |

---

## 九、执行与验证步骤

### 9.1 全新环境（推荐）

```powershell
cd D:\Code\ai_risk

# 1. 建库建表 + 基础数据（旅游字典 + 30 规则 + 5 黑名单）
python scripts/init_db.py --reset

# 2. 大规模旅游模拟数据（默认 1w 用户 ≈ 4.5w 订单, 满足 ≥500 条要求）
python scripts/gen_10w_data.py

# 3. 30 个高风险样本用户（5 种旅游风险模式轮换 × 6 套 = RISK001-RISK030）
python scripts/gen_risky_users.py --count 30

# 4. 生成训练样本（默认 30 RISK + 30 正常用户 × 25 事件/人 = 1500 条）→ 训练 XGBoost → 回填画像
python scripts/gen_train_dataset.py
python scripts/train_xgb_model.py
python scripts/backfill_ml_score.py

# 5. 启动服务
python run_app.py
```

### 9.2 老电商库升级

```powershell
mysql -uroot -p123456 ai_risk < sql/migration_travel_industry.sql
python scripts/gen_10w_data.py
python scripts/gen_risky_users.py --count 30
python scripts/backfill_ml_score.py
```

### 9.3 端到端验证

```powershell
# 风控检查 API（预订事件, source_id 为订单号）
curl -X POST http://localhost:8000/api/risk/check -H "Content-Type: application/json" -d "{\"event_type\":\"预订\",\"user_id\":\"RISK001\",\"source_id\":\"ORD_001_01\",\"source\":\"api\"}"

# 规则列表（分页）
curl "http://localhost:8000/api/rules?page_size=50"

# 仪表盘概览
curl http://localhost:8000/api/dashboard/overview
```

> 注：风控检查请求体字段为 `event_type` / `user_id` / `source_id`（订单号或退改签号）/ `source`，
> 其中 `source_id` 必填；`退改签` 事件的 source_id 传退改签单号（postsale_id）。

页面验证：`http://localhost:8000/` —— 仪表盘 / 风控检查 / 规则管理 / 评估记录 /
黑名单 / 案件管理 / AI 助手，全部标题与下拉均为旅游术语。

### 9.4 单元测试

```powershell
# 使用项目 uv 虚拟环境
uv\.venv\Scripts\python.exe -m pytest tests/ -v
```

> 说明：`test_pagination.py` / `test_ml_score_sigmoid_frontend.py` 依赖 Node.js 运行前端 JS，
> 若未安装 Node.js 会自动 skip（不影响其余用例）。

---

## 十、注意事项

1. **表名未改**：`receive_info`（出行人/行程）、`postsale`（退改签）、`logistics`（行程）
   沿用原表名，仅语义迁移，避免大范围 ORM/路由改名风险。
2. **`order_lead_days`** 为旅游行业新增特征（出行提前期），依赖 `order_info.travel_date`，
   老库升级脚本会自动补列。
3. **模型需重训**：特征语义变化后，务必按 9.1 第 4 步重训 XGBoost，
   否则 `app/engine/xgb_model.json` 与特征列不一致。
4. **密码**：root=`123456`（`.env`：DB_PASSWORD=123456），应用账号 `app`/`app123`。
5. 历史迁移脚本 `migration_add_p4_tables.sql` / `migration_add_2026_08_07_fields.sql` /
   `migration_add_case_source_id.sql` 中的 ENUM 已同步为旅游取值，幂等可重复执行，
   但新环境无需运行（init_risk_tables.sql 已包含全部列）。
